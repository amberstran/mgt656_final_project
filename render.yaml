services:
  # Backend Django API
  - type: web
    name: agora-backend
    env: python
    buildCommand: pip install -r requirements.txt && cd backend && python manage.py collectstatic --noinput
    startCommand: cd backend && gunicorn agora_backend.wsgi:application --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: False
      - key: DATABASE_URL
        fromDatabase:
          name: agora-db
          property: connectionString
      - key: ALLOWED_HOSTS
        sync: false
      - key: CORS_ALLOWED_ORIGINS
        sync: false
      - key: CSRF_TRUSTED_ORIGINS
        sync: false

  # Frontend React App (Static Site)
  - type: static
    name: agora-frontend
    buildCommand: cd backend/agora_frontend && npm install && npm run build
    staticPublishPath: backend/agora_frontend/build
    envVars:
      - key: NODE_VERSION
        value: 20.18.0
      - key: REACT_APP_API_URL
        sync: false

databases:
  - name: agora-db
    databaseName: agora_db
    user: agora_user
    plan: free

